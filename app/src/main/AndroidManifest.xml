<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ninjawaffle.myapplication">
    <uses-permission android:name="android.permission.CALL_PHONE" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_notext"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity
            android:name=".Homepage"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".InsertDetailsActivity">
        </activity>

        <activity android:name=".ChooseOptionActivity">

        </activity>
        <activity android:name=".InsertMedicareActivity">


        </activity>
    </application>
</manifest>

